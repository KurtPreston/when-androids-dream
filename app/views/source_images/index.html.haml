%h1 Listing source_images

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @source_images.each do |source_image|
      %tr
        %td= link_to 'Show', source_image
        %td= link_to 'Edit', edit_source_image_path(source_image)
        %td= link_to 'Destroy', source_image, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Source image', new_source_image_path
